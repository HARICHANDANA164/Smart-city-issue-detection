# Backend configuration (local dev)

# Where to read/write the ML model artifacts (2 models)
CATEGORY_MODEL_PATH=../models/artifacts/category_model.joblib
URGENCY_MODEL_PATH=../models/artifacts/urgency_model.joblib

# Training data path used ONLY if model artifacts don't exist yet
# Expected columns: complaint_type, descriptor, category, urgency_label
TRAIN_DATA_PATH=../data/nyc_311_subset.csv

# Persistence (SQLite). For AWS Lambda you'll likely swap this to DynamoDB.
DB_PATH=../data/complaints.db

# CORS
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173


